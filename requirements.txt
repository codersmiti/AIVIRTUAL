# Streamlit UI and utilities
streamlit
gdown
pillow
numpy==1.26.4
opencv-python
scikit-image

# PyTorch core + vision (Python 3.12 compatible)
torch==2.2.2
torchvision==0.17.2

# U2Net (image segmentation)
scipy

# Human parsing (Parsing- repo uses torchvision transforms, PIL, argparse)
matplotlib

# For JSON pose keypoints and text I/O
json5

