streamlit
gdown
pillow
numpy<2.0
opencv-python
torch==2.5.1
torchvision==0.18.1
scikit-image
# U2Net (image segmentation)
scipy

# Human parsing (Parsing- repo uses torchvision transforms, PIL, argparse)
matplotlib

# For JSON pose keypoints and text I/O
json5
